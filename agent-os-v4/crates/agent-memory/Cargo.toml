[package]
name = "agent-memory"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
agent-core = { path = "../agent-core" }
